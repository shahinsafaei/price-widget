# نسخه 2.9.5 — بازطراحی صفحه اصلی

## چرا این آپدیت
صفحه اصلی قبلی همه‌ی ده‌ها آیتم API رو با وزن بصری یکسان نشون می‌داد. این نسخه
مفهوم «واچ‌لیست شخصی» رو از «انتخاب ویجت» جدا می‌کنه تا صفحه اصلی مرتب و
متمرکز بشه.

## تغییرات اصلی

### مفهوم جدید: صفحه اصلی جدا از ویجت
- **آیتم‌های صفحه اصلی** (بدون محدودیت تعداد) از **آیتم‌های ویجت** (حداکثر ۳ تا،
  مثل قبل) کاملاً جدا شدن. کاربر می‌تونه ۴، ۵ یا بیشتر آیتم روی صفحه اصلی
  اپ دنبال کنه ولی فقط ۳ تا رو روی ویجت هوم‌اسکرین گوشی ببینه.
- کاربر جدید به‌صورت پیش‌فرض ۴ آیتم می‌گیره: **دلار، یورو، طلای ۱۸ عیار،
  بیت‌کوین**.

### صفحه اصلی
- **کارت Featured به‌صورت Pager قابل swipe** بین همه‌ی آیتم‌های واچ‌لیست، با
  نقطه‌های وضعیت (dot indicator) زیرش.
- **خط خلاصه‌ی روزانه** بالای صفحه: «امروز X از Y مورد رشد داشتن» + آیتمی که
  بیشترین تغییر رو داشته.
- **گرید ۲ ستونه‌ی کارت‌های بزرگ** برای واچ‌لیست، هر کارت با دکمه‌ی حذف سریع (×).
- **دکمه‌ی «افزودن آیتم»** پایین گرید که یک صفحه‌ی مدیریت تمام‌صفحه باز می‌کنه.
- **حالت خالی هوشمند**: اگه کاربر همه‌چیز رو حذف کنه، یک دکمه‌ی تک‌کلیکی برای
  برگردوندن ۴ آیتم پیش‌فرض نشون داده می‌شه.

### صفحه‌ی «مدیریت آیتم‌ها» (جایگزین لیست قبلی)
- همون فیلتر دسته‌بندی و لیست کامل قبلی، ولی حالا هر ردیف دو دکمه‌ی جدا داره:
  - **☆/★ ستاره** → افزودن/حذف از صفحه اصلی (بدون محدودیت)
  - **☐/✓ تیک** → افزودن/حذف از ویجت (حداکثر ۳ تا، مثل قبل)

## نسخه
- versionName: 2.9.3 → **2.9.5**
- versionCode: 8 → **9**

## نکات فنی برای بیلد
- از `HorizontalPager` و `rememberPagerState` در `androidx.compose.foundation.pager`
  استفاده شده (نیاز به `@OptIn(ExperimentalFoundationApi::class)` که در کد
  گذاشته شده — هشدار experimental موقع کامپایل طبیعیه، خطا نیست).
- یک کلید جدید DataStore اضافه شده: `home_items` (در `PrefsRepository.kt`).
  کلید قبلی `selected_items` دست‌نخورده باقی مونده و همچنان برای ویجت استفاده
  می‌شه — یعنی کاربرهای قبلی هیچ‌چیزی از انتخاب ویجتشون رو از دست نمی‌دن.

## آنچه در نسخه‌ی بعد می‌مونه (طبق صحبت‌های قبلی)
- بازطراحی ظاهر خود ویجت (سبک Bold Minimal: پس‌زمینه پررنگ، نوار رنگی کنار
  هر ردیف، تایپوگرافی کنتراستی، آیکون خطی مونوکروم)
- آیکون‌های خطی سفارشی برای اپ و ویجت (نیاز به دانلود یک آیکون‌ست مثل
  Lucide/Phosphor)
- Motion/spring animation روی انتخاب کارت
