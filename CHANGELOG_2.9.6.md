# نسخه 2.9.6 — بهبود هدر منتخب و صفحه مدیریت آیتم‌ها

## هدر آیتم‌های منتخب (Featured Pager)
- **Peek preview**: لبه‌ی کارت قبلی/بعدی کمی از کنار دیده می‌شه (با افزایش padding
  اطراف Pager)، تا معلوم باشه با swipe آیتم دیگه‌ای هم هست.
- **شمارنده‌ی موقعیت** («۲ / ۴») کنار نقطه‌های وضعیت.
- **هاله‌ی رنگی محیطی (ambient)** پشت کل بخش هدر که هنگام swipe بین یک آیتم
  سبز و یک آیتم قرمز، به‌آرومی رنگش crossfade می‌شه (بر پایه‌ی
  `pagerState.currentPageOffsetFraction`).
- **دکمه‌ی ستاره روی خود کارت بزرگ** برای حذف مستقیم از صفحه اصلی، بدون نیاز
  به رفتن به گرید پایین یا صفحه‌ی مدیریت.

## صفحه‌ی «مدیریت آیتم‌ها» — بازطراحی کامل
- **دو تب جدا**: «صفحه اصلی» و «ویجت (X/۳)» — به‌جای دو دکمه‌ی کنار هم، هر
  مفهوم تب خودش رو داره تا گیج‌کننده نباشه.
- **کلید روشن/خاموش (Switch)** استاندارد به‌جای نمادهای ★/✓ سفارشی — فوری‌تر
  قابل فهمه.
- **نوار جستجو** بالای لیست برای پیدا کردن سریع آیتم در لیست بلند.

## گرید صفحه اصلی
- **ترتیب دلخواه**: هر کارت دو فلش کوچیک (‹ ›) داره که کاربر می‌تونه با
  کلیک، ترتیب آیتم‌ها رو در صفحه اصلی (و در نتیجه ترتیب Pager بالا) خودش
  دستی تغییر بده. ترتیب ذخیره و بعد از بستن اپ هم حفظ می‌شه.

## تغییر فنی مهم
- ذخیره‌سازی «آیتم‌های صفحه اصلی» در `PrefsRepository` از یک `Set` بدون‌ترتیب
  به یک **لیست مرتب** (`home_items_order`، جدا شده با یک کاراکتر Unicode
  خاص) تغییر کرد تا امکان reorder دستی فراهم بشه. کلید قدیمی (`home_items`)
  دیگه استفاده نمی‌شه — یعنی کاربرهای قبلی، لیست صفحه اصلی‌شون یک‌بار به حالت
  پیش‌فرض (دلار/یورو/طلای ۱۸/بیت‌کوین) برمی‌گرده و بعد قابل تغییره. انتخاب
  ویجت (`selected_items`) دست‌نخورده و مستقل باقی مونده.

## نسخه
- versionName: 2.9.5 → **2.9.6**
- versionCode: 9 → **10**

## بررسی کیفیت قبل از تحویل
- تعادل آکولاد/پرانتز کل فایل به‌صورت خودکار چک شد (۲۷۲/۲۷۲ و ۷۲۷/۷۲۷)
- کل فایل برای همون نوع خطای قبلی (ترکیب غلط پارامترهای `padding()`) دوباره
  اسکن شد — چیزی پیدا نشد
- همه‌ی امضای APIهای جدید (`TabRow`, `Tab`, `OutlinedTextField`, `Switch`,
  `pagerState.currentPageOffsetFraction`, `androidx.compose.ui.graphics.lerp`)
  با نسخه‌ی Compose این پروژه (BOM 2024.06.00) تطبیق داده شد
