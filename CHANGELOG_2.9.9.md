# نسخه 2.9.8

## کارت Featured
- پرچم بزرگ حالا با ردیف **عدد قیمت** هم‌تراز شده (نه با اسم/درصد بالا) —
  قبلاً وسط کارت بود و پشت اسم گم می‌شد.
- **ورق خوردن خودکار هر ۵ ثانیه** بین آیتم‌های هدر (Pager) — کاربر می‌تونه
  دستی هم swipe کنه، ولی اگه دست نزنه خودش می‌ره سراغ بعدی.

## اعتبار توسعه‌دهنده
- آیدی «@shahinsafaei» حذف شد، فقط «توسعه‌دهنده: شاهین صفایی» نمایش داده
  می‌شه.
- آیکون به یک نماد واقعی اینستاگرام (فایل vector جدید `ic_instagram.xml`) تغییر کرد.
- کل ردیف (آیکون + متن) قابل کلیکه و به اینستاگرام می‌بره — همون رفتار قبلی،
  فقط ظاهرش تمیزتر شد.

## نسخه
- versionName: 2.9.7 → **2.9.8**
- versionCode: 11 → **12**

## بررسی کیفیت
- تعادل آکولاد/پرانتز کل فایل صفر خطا (۲۸۷/۲۸۷ و ۸۰۳/۸۰۳)
- اسکن کامل برای الگوی خطای padding قبلی — چیزی پیدا نشد

## اصلاحیه (همون نسخه 2.9.8)
- **پرچم کارت Featured**: کامل از پشت متن دراومد. الان در یک لِین جدا و
  مستقل کنار متن قرار داره (نه پشت اسم، نه پشت عدد) — دیگه هیچ‌وقت با متن
  همپوشانی نداره.
- **اعلان‌ها و افزودن ویجت**: به‌جای دو ردیف زیر هم، حالا دو کاشی مربعی
  کنار هم هستن (شبیه کارت‌های گرید) — هر کدوم آیکون دایره‌ای، عنوان، و
  کنترلش (سوییچ یا فلش).
- گرید صفحه اصلی و کاشی «افزودن» کمی جمع‌وجورتر شدن (پدینگ و اندازه‌ها
  کاهش پیدا کرد).
- عدد قیمت داخل کارت‌های گرید بزرگ‌تر شد (titleMedium → titleLarge).
- پرچم/نماد داخل کارت‌های گرید حدود ۱۰٪ بزرگ‌تر شد (۲۲sp → ۲۴sp).

## نسخه 2.9.9 — رفع باگ بحرانی
- **ارتفاع غول‌آسای کارت Featured**: علتش این بود که پرچم از `fillMaxHeight()`
  استفاده می‌کرد ولی خود کارت فقط `heightIn(min=...)` داشت (بدون سقف مشخص)
  — این ترکیب باعث می‌شد کارت بخواد کل فضای عمودی موجود (تقریباً کل صفحه) رو
  پر کنه. با `Modifier.height(IntrinsicSize.Min)` (همون تکنیک امنی که برای
  ردیف‌های گرید و کارت‌های تنظیمات هم استفاده شده) حل شد — کارت حالا دقیقاً
  به اندازه‌ی محتوای خودشه.
- پس‌زمینه‌ی رنگی جداگانه‌ی پشت پرچم حذف شد (طبق نظر کاربر، شکل "استیکر
  جعبه‌ای" داشت) — الان فقط خود نماد پرچم بدون قاب دیده می‌شه.
- تمام جاهای دیگه‌ی فایل که از `fillMaxHeight()` استفاده می‌کنن (کاشی‌های
  گرید، تنظیمات) بررسی و تایید شد که همگی داخل یک `Row` با
  `IntrinsicSize.Min` هستن — یعنی این باگ جای دیگه‌ای تکرار نشده.

## نسخه
- versionName: 2.9.8 → **2.9.9**
- versionCode: 12 → **13**

## بررسی کیفیت
- تعادل آکولاد/پرانتز کل فایل صفر خطا (۲۸۶/۲۸۶ و ۸۰۸/۸۰۸)
