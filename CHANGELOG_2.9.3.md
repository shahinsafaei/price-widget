# نسخه 2.9.3

## رفع باگ
- **رفع باگ برعکس‌شدن اعداد قیمت وقتی زبان گوشی فارسی است.**
  علت: در `RollingNumberText` هر رقم یک `Text` جدا داخل یک `Row` است؛ وقتی
  جهت‌ چیدمان سیستم به‌خاطر زبان فارسی RTL می‌شود، خود `Row` ترتیب فرزندهاش
  (ارقام) را برعکس می‌چیند و عدد `32,363,000` به شکل `000,363,32` نمایش داده
  می‌شد. این `Row` حالا صریحاً روی `LayoutDirection.Ltr` قفل شده. همین مشکل در
  متن قیمت داخل ویجت (Glance) و نوتیفیکیشن هم با اضافه‌کردن نشانه‌های
  Unicode Isolate (U+2066 / U+2069) دور رشته‌ی عدد رفع شد.

## تغییرات ظاهری
- نشانگر «بازار باز / بسته» کنار عنوان اپ (بر اساس بازه‌ی ۹ تا ۲۰).
- چیپ‌های فیلتر دسته (طلا و سکه / ارز / ارز دیجیتال) بالای لیست.
- حالت خطا و حالت خالی با آیکون، توضیح و دکمه‌ی «تلاش دوباره».
- بازخورد لرزشی (haptic) هنگام رفرش، وقتی قیمتی واقعاً تغییر کرده باشد.
- دیالوگ خوش‌آمدگویی یک‌باره برای کاربر تازه، که نحوه‌ی انتخاب آیتم برای
  ویجت را توضیح می‌دهد.

## نسخه
- versionName: 2.9 → **2.9.3**
- versionCode: 7 → **8**

## پیشنهادهایی که پیاده نشدند (نیاز به تصمیم/دادهٔ بیشتر دارند)
این‌ها را عمداً پیاده نکردم چون یا نیاز به API/دادهٔ تاریخچه‌ای دارند که
سرویس فعلی (`brsapi.ir`) در پاسخش نمی‌دهد، یا تغییر معماری بزرگ‌تری لازم دارند:
- نمودار sparkline واقعی روی کارت Hero (نیاز به سری قیمت تاریخی).
- حالت مقایسه‌ی دو آیتم کنار هم (drag/long-press).
- سایز سوم (Medium) برای ویجت adaptive.
- صفحه‌ی جدید «روند» در bottom navigation.
اگر بخوای، می‌تونم هر کدوم از این‌ها رو هم در یک مرحله‌ی بعدی پیاده کنم.
